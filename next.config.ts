import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
    /* config options here */
    output: 'standalone',
    allowedDevOrigins: ['109.196.102.221', 'https://khalimendik.ru'],
};

export default nextConfig;
